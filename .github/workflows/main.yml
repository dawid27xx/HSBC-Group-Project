# .github/workflows/tests.yml
name: Push Tests

on:
  pull_request:
    branches: 
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Install dependencies
        run: npm ci

      - name: Run Jest tests
        run: npm test
